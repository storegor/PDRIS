apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: storegor-ns
spec:
  type: NodePort
  selector:
    app: web
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 5000
      nodePort: 30007
